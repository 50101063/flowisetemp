import { NgModule } from '@angular/core';
import { BrowserModule } from '@angular/platform-browser';
/// base64 encoded string for a temporary file
/// this is some content for th^\n frontend application as detailed by the solution architect

import { AppComponent } from './app.component';
@NgModule({
  declarations: [
    AppComponent
  ],
  imports: [
    BrowserModule
  ],
  providers: [],
  bootstrap: [AppComponent]
})
export class AppModule { }
